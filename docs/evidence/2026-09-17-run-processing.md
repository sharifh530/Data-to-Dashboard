# B06 synthetic orchestration evidence

Date: 2026-09-17. Windows, Python 3.13.7, PostgreSQL 17.6, Node 24.20.0. Tests use generated isolated schemas and synthetic data.

Implemented transactional admission/outbox, idempotency, one active run per owner, leased work items, fenced checkpoint/publication, bounded recovery, history/detail, SSE replay/snapshot and cancellation. Migration 48b57ba1bfea preserves existing event sequences. ADR-017 selects PostgreSQL delivery over proposed Redis/Celery.

Validation commands and outcomes:

- `npm run contracts:generate`: generated OpenAPI/TypeScript.
- `npm run db:migrate`: local migration successful.
- `npm run check`: passed lint, TypeScript, 4 protocol tests, Ruff, strict mypy (18 source files), Python (48 passed/28 PostgreSQL skipped without database environment), contract drift and fixture build.
- `npm run test:postgres`: 76 passed, no skips; two existing upstream test-client deprecation warnings.
- `npm run db:check`: no new upgrade operations.
- `npm run dev:worker -- --once`: successful local PostgreSQL worker startup/exit.

Tests cover duplicate delivery/admission, concurrent claims, cross-user denial, CSRF, event IDs/replay/snapshot, session revocation, stale completion/heartbeat, retry/deadline bounds, unsupported configuration, output validation and owner/input tombstones. A separate process commits a checkpoint and exits with leased work; replacement processing resumes after simulated expiry. Concurrent cancellation/publication accepts only the winning terminal result. Migration downgrade/upgrade preserves event sequence values.

Initial lint failed because `.kilo/worktrees` introduced a second TypeScript configuration. Final configuration excludes tooling worktrees and explicitly sets the parser root; worktree contents were untouched. An autogenerated migration import-order error was fixed before final checks.

Limits: lease expiry is simulated by timestamp updates. No sandbox process termination, load benchmark, external provider, LangGraph, uploaded-file parsing or generated Python/React execution is claimed. Renderer unchanged; browser tests not rerun. Remote CI results not inspected. B01 remains blocked on verified hardened Linux execution.
